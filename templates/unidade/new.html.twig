{% extends 'base/main.html.twig' %}


{% block page_title %}

    <section class="content-header">
        <h1>
            Unidade
            <small>Novo</small>
        </h1>
    </section>

{% endblock %}


{% block first_row %}

    <div class="row">

        <div class="col-md-2 col-sm-3 col-xs-3">

            <a href="{{ path('cadastro_unidade_index') }}" class="btn btn-{{ button_color }} ">
                <i class="ti ti-arrow-left"></i>
            </a>

        </div>
    </div>{% endblock %}


    {% block card_body %}

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <label class="form-label"></label>
        <button type="submit" class="btn btn-{{ button_color }} ">
            <i class="ti ti-device-floppy"></i>
        </button>
        {{ form_end(form) }}

    {% endblock %}
