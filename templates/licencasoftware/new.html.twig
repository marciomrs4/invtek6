{% extends 'base/main.html.twig' %}


{% block page_title %}

        <section class="content-header">
            <h1>
                Licença Software
                <small>Novo</small>
            </h1>
        </section>

    {% endblock %}


{% block first_row %}

        <div class="row">

            <div class="col-md-2 col-sm-2 col-xs-2">
                <a href="{{ path('cadastro_licencasoftware_index', { 'software': software.id }) }}" class="btn btn-{{ button_color }}">
                    <span class="ti ti-arrow-left"></span>
                </a>
            </div>

            <div class="col-md-2 col-sm-2 col-xs-2">
                <a href="{{ path('cadastro_software_show', { 'id': software.id }) }}" class="btn btn-{{ button_color }}" title="Mostrar Software">
                    <span class="ti ti-arrow-up"></span>
                </a>
            </div>

        </div>
    {% endblock %}


{% block card_body %}

        {{ form_start(form) }}
        {{ form_widget(form) }}
        
        <span class="form-label"></span>
        <button type="submit" class="btn btn-{{ button_color }}">
            <span class="ti ti-device-floppy"></span>
        </button>
        {{ form_end(form) }}

    {% endblock %}
